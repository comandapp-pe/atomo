<%= form_with(model: font) do |form| %>
  <% if font.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(font.errors.count, "error") %> prohibited this font from being saved:</h2>

      <ul>
        <% font.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
