<main class="container p-4">
  <h1>Nueva frase comercial</h1>

  <%= render partial: 'phrases/form', phrase: @phrase, locals: { order: @order, phrase: @phrase } %>

  <br>

  <%= link_to 'Regresar', order_phrases_path(@order) %>
</main>