{
    $('#new-video').html("<%= j render partial: 'videos/form', locals: { order: @order } %>")

    <% if @video %>
    $('#all-videos').append("<%= j render partial: 'videos/video', locals: { video: @video } %>")
    <% end %>
}