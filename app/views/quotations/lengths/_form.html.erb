<%= form_with(model: quotations_length) do |form| %>
  <% if quotations_length.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(quotations_length.errors.count, "error") %> prohibited this quotations_length from being saved:</h2>

      <ul>
        <% quotations_length.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
