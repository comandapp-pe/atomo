<%= form_with(model: quotations_lead) do |form| %>
  <% if quotations_lead.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(quotations_lead.errors.count, "error") %> prohibited this quotations_lead from being saved:</h2>

      <ul>
        <% quotations_lead.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
