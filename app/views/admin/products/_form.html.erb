<%= form_with model: [:admin, product],  class: 'mt-3' do |form| %>
  <% if product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(product.errors.count, "error") %> evitaron guardar este producto:</h2>

      <ul>
        <% product.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="mb-2">
    <%= form.label :name, 'Nombre', class: 'form-label' %>
    <%= form.text_field :name, class: 'form-control' %>
  </div>

  <div class="mb-2">
    <%= form.label :external_reference, 'Código de 2Checkout', class: 'form-label' %>
    <%= form.text_field :external_reference, class: 'form-control' %>
  </div>

  <div class="mb-2">
    <%= form.label :preview_url, 'URL de prevista', class: 'form-label' %>
    <%= form.text_field :preview_url, class: 'form-control' %>
  </div>

  <%= form.submit class: 'btn btn-primary mt-3' %>
<% end %>
