<%= render 'admin/header' %>

<main class="container p-4">
  <% if notice %>
    <div id="notice" class="alert alert-success alert-dismissible fade show">
      <%= notice %> <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
    </div>
  <% end %>

  <div>
    <%= link_to 'Crear Producto', new_admin_product_path, class: 'btn btn-primary' %>
  </div>

  <br>

  <div class="card">
    <h1 class="card-header h3">Productos</h1>

    <div class="card-body">
      <% if @products.empty? %>
        <div class="alert alert-primary mb-0">
          No hay datos disponibles.
          <hr>
          Crea un producto.
        </div>
      <% else %>
        <div class="row gy-3">
          <% @products.each do |product| %>
            <div class="col-12">
              <%= link_to [:admin, product], class: 'card text-decoration-none col-12' do %>
                <div class="card-body">
                  <div class="card-title"><%= product.name %></div>
                  <div class="card-subtitle text-muted">
                    <% if product.category_id? %>
                      <span><%= product.category.name %></span>
                    <% else %>
                      <span class="text-danger">Sin categoría</span>
                    <% end %>
                    <span class="text-dark">|</span>
                    <%= product.published ? 'Publicado' : 'No publicado' %>
                  </div>
                </div>
              <% end %>
            </div>
          <% end %>
        </div>
      <% end %>
    </div>
  </div>
</main>